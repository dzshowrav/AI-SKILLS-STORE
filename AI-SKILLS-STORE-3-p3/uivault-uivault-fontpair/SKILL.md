---
name: Fontpair
description: UI Vault resource — Color & Typography Tools
source: https://fontpair.co/
category: Color & Typography Tools
type: external-resource
---

# Fontpair

> Color & Typography Tools · [Visit website](https://fontpair.co/)

## Overview

This is a curated resource from [UI Vault](https://en970.github.io/ui-vault/) — a hand-picked collection of premium UI design resources.

**Category:** Color & Typography Tools

**Website:** https://fontpair.co/

## Reference Content

The following content was fetched from the resource's website:

Fontpair - Free Google Font Combinations &amp; Generator /* FOUC prevention: hide page until CSS loads */ html:not(.css-loaded) body { visibility: hidden; } html.css-loaded body { visibility: visible; } /* Minimal reset + base styles */ *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; } html { -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; } html, body { overscroll-behavior-y: none; } body { font-family: 'Geist', system-ui, -apple-system, sans-serif; background: hsl(0 0% 97%); color: hsl(222.2 84% 4.9%); line-height: 1.5; } /* Size-adjust fallback to reduce font swap CLS */ @font-face { font-family: 'Geist Fallback'; src: local('system-ui'), local('-apple-system'), local('Segoe UI'), local('sans-serif'); size-adjust: 100%; ascent-override: 90%; descent-override: 22%; line-gap-override: 0%; } /* Dark mode - matches index.css exactly */ html.dark body { background: hsl(0 0% 7%); color: hsl(0 0% 98%); } /* Loading spinner for empty root */ #root:empty { display: flex; align-items: center; justify-content: center; min-height: 100vh; } #root:empty::after { content: ''; display: block; width: 32px; height: 32px; border: 2px solid hsl(214.3 31.8% 91.4%); border-top-color: hsl(222.2 84% 4.9%); border-radius: 50%; animation: spin 0.8s linear infinite; } html.dark #root:empty::after { border-color: hsl(217.2 32.6% 17.5%); border-top-color: hsl(210 40% 98%); } @keyframes spin { to { transform: rotate(360deg); } } /* Critical layout styles to prevent layout shift */ .min-h-screen { min-height: 100vh; } .flex { display: flex; } .flex-col { flex-direction: column; } .items-center { align-items: center; } .justify-center { justify-content: center; } // Add css-loaded class once document is ready to prevent FOUC (function() { // Check if we have cached theme preference var theme = localStorage.getItem('theme'); if (theme === 'dark' || (!theme && window.matchMedia('(prefers-color-scheme: dark)').matches)) { document.documentElement.classList.add('dark'); } // Mark CSS as loaded after a minimal delay to ensure styles are parsed if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', function() { requestAnimationFrame(function() { document.documentElement.classList.add('css-loaded'); }); }); } else { requestAnimationFrame(function() { document.documentElement.classList.add('css-loaded'); }); } })(); // Load analytics after the page is interactive function loadDeferredScripts() { // Google Analytics — defer until first user interaction or idle fallback // to keep ~215ms of CPU off the critical render path. window.dataLayer = window.dataLayer || []; function gtag(){dataLayer.push(arguments);} gtag('js', new Date()); gtag('config', 'G-2Z27F6J9SE'); var gaLoaded = false; function loadGA() { if (gaLoaded) return; gaLoaded = true; var ga = document.createElement('script'); ga.src = 'https://www.googletagmanager.com/gtag/js?id=G-2Z27F6J9SE'; ga.async = true; document.head.appendChild(ga); } var gaInteractionEvents = ['pointerdown', 'keydown', 'scroll', 'touchstart']; gaInteractionEvents.forEach(function(ev) { window.addEventListener(ev, loadGA, { once: true, passive: true }); }); // Fallback: load GA after a short idle window if no interaction occurs setTimeout(loadGA, 3000); // Visitors Analytics - use async and append to body to avoid render blocking var v = document.createElement('script'); v.src = 'https://cdn.visitors.now/v.js'; v.async = true; v.defer = true; v.dataset.token = '892b4440-4e61-4493-871c-058fa8afaf3c'; document.body.appendChild(v); // Microsoft Clarity — fully decoupled from other deferred scripts. // Clarity queries layout properties (offsetWidth, getBoundingClientRect) // on init and during session recording, causing forced reflows that // Lighthouse attributes ~250ms to during the critical loading window. // We push it well past LCP/TTI: only after first user interaction, // with a long idle fallback, and isolated in its own scheduling tick // so it never competes with GA, fonts, or other deferred work. var clarityLoaded = false; function loadClarity() { if (clarityLoaded) return; clarityLoaded = true; // Schedule the actual injection in a separate idle callback so the // reflow-heavy Clarity bootstrap runs in its own task, well after // the main thread has settled from LCP/TTI work. var inject = function() { (function(c,l,a,r,i,t,y){ c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)}; t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i; y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y); })(window, document, "clarity", "script", "r416fw1j1o"); }; if ('requestIdleCallback' in window) { requestIdleCallback(inject, { timeout: 4000 }); } else { setTimeout(inject, 0); } } var clarityInteractionEvents = ['pointerdown', 'keydown', 'scroll', 'touchstart']; clarityInteractionEvents.forEach(function(ev) { window.addEventListener(ev, loadClarity, { once: true, passive: true }); }); // Fallback: load Clarity after a long idle window (8s) if no interaction. // This is intentionally longer than other deferred scripts so Clarity // doesn't compete with GA / font loads on slower devices. setTimeout(loadClarity, 8000); // Load additional popular Google Fonts after user interaction // Using Latin subset to reduce file sizes while maintaining full character support var fontsLoaded = false; var loadFonts = function() { if (fontsLoaded) return; fontsLoaded = true; // Common preview characters for font cards - subset to reduce size by ~70% var previewChars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789.,!?\'-"&'; var encodedChars = encodeURIComponent(previewChars); // Load preview-optimized fonts (subsetted) for card display var previewLink = document.createElement('link'); previewLink.rel = 'stylesheet'; previewLink.href = 'https://fonts.googleapis.com/css2?family=Inter:wght@400;700&family=Roboto:wght@400;700&family=Lato:wght@400;700&family=Open+Sans:wght@400;700&family=Montserrat:wght@400;700&family=Poppins:wght@400;700&family=Playfair+Display:wght@400;700&family=Merriweather:wght@400;700&text=' + encodedChars + '&display=swap'; document.head.appendChild(previewLink); // Load full Latin subset for fonts that need complete character support (body text) var fullLink = document.createElement('link'); fullLink.rel = 'stylesheet'; fullLink.href = 'https://fonts.googleapis.com/css2?family=Source+Sans+3:wght@400;600;700&family=Nunito:wght@400;600;700&family=Work+Sans:wght@400;600;700&family=Raleway:wght@400;600;700&family=Oswald:wght@400;600;700&family=Quicksand:wght@400;600;700&family=Josefin+Sans:wght@400;600;700&family=Alegreya+Sans:wght@400;700&family=Bitter:wght@400;700&family=Caudex:wght@400;700&family=Cambo:wght@400&subset=latin&display=swap'; document.head.appendChild(fullLink); }; // Load fonts on first user interaction or after 2 seconds ['scroll', 'mousemove', 'touchstart', 'keydown'].forEach(function(evt) { window.addEventListener(evt, loadFonts, { once: true, passive: true }); }); setTimeout(loadFonts, 2000); // Fontshare fonts: load on font/pairing/category pages // Note: Fontshare doesn't support text subsetting, but we minimize weights var path = window.location.pathname; var needsFontshare = path !== '/' && ( path.includes('/fonts') || path.includes('/pairing') || path.includes('/all') || path.includes('/category') ); if (needsFontshare) { var fontshare = document.createElement('link'); fontshare.rel = 'stylesheet'; // Only load the weights actually used in previews (400, 700) fontshare.href = 'https://api.fontshare.com/v2/css?f[]=satoshi@400,700&f[]=clash-display@400,700&f[]=cabinet-grotesk@400,700&f[]=general-sans@400,700&f[]=zodiak@400,700&f[]=author@400,700&display=swap'; document.head.appendChild(fontshare); } } // Use requestIdleCallback for best performance, fallback to load event if ('requestIdleCallback' in window) { requestIdleCallback(loadDeferredScripts, { timeout: 2000 }); } else { window.addEventListener('load', l...
[Content truncated]

## How to Use This Skill

When working with Fontpair, reference this skill to:
- Understand the core features and capabilities
- Find the official documentation and examples
- Apply best practices from the UI Vault curation

## Related Resources

Visit the full [UI Vault](https://en970.github.io/ui-vault/) for more resources in the Color & Typography Tools category.

---
*This skill was auto-generated from [Fontpair](https://fontpair.co/) — a UI Vault curated resource.*
