---
name: Kite
description: UI Vault resource — Mockup & Promo Video Tools
source: https://kite.video/
category: Mockup & Promo Video Tools
type: external-resource
---

# Kite

> Mockup & Promo Video Tools · [Visit website](https://kite.video/)

## Overview

This is a curated resource from [UI Vault](https://en970.github.io/ui-vault/) — a hand-picked collection of premium UI design resources.

**Category:** Mockup & Promo Video Tools

**Website:** https://kite.video/

## Reference Content

The following content was fetched from the resource's website:

Kite – Screen recorder for stunning demo videos --> // Google Consent Mode v2 defaults — must be set synchronously before // gtag.js loads; iubenda updates these on consent via googleConsentMode window.dataLayer = window.dataLayer || []; function gtag() { dataLayer.push(arguments); } gtag("consent", "default", { ad_storage: "denied", ad_user_data: "denied", ad_personalization: "denied", analytics_storage: "denied", // iubenda_cs loads on window load, so give early gtag hits time to // wait for the consent update instead of firing as denied wait_for_update: 8000, }); var _iub = _iub || []; _iub.csConfiguration = { siteId: 3846428, cookiePolicyId: 24275151, cookiePolicyUrl: "https://kite.video/cookies", storage: { useSiteId: true }, lang: "en", countryDetection: true, // We run no on-site programmatic ads, so the IAB TCF is unnecessary; // it also injects the "1188 third parties" disclosure into the banner. // Explicitly off here in case the dashboard-side setting re-enables it. enableTcf: false, perPurposeConsent: true, consentOnContinuedBrowsing: false, // NOTE: do not set googleConsentMode: "template" here — it disables // the autoblocking engine (see the init gate in // cs.iubenda.com/autoblocking/3846428.js). That engine pushes its own // Google Consent Mode update, but its purpose mapping is unreliable // (observed granting/denying the wrong flags), so the callback below // pushes an explicit update afterwards — last update wins per flag. callback: { // iubenda purposes: 3 = experience, 4 = measurement, 5 = advertising onPreferenceExpressedOrNotNeeded: function (preference) { var granted = function (g) { return g ? "granted" : "denied"; }; var purposes = preference && preference.purposes; var measurement, marketing; if (purposes) { // GDPR-style per-purpose consent measurement = preference.consent === true || !!purposes[4]; marketing = preference.consent === true || !!purposes[5]; } else { // Consent not required (or US opt-out framework): granted // unless the visitor opted out var optedOut = false; try { optedOut = window._iub.cs.api.isCcpaOptedOut(); } catch (e) {} measurement = !optedOut; marketing = !optedOut; } gtag("consent", "update", { analytics_storage: granted(measurement), ad_storage: granted(marketing), ad_user_data: granted(marketing), ad_personalization: granted(marketing), }); }, }, banner: { position: "float-bottom-center", acceptButtonDisplay: true, rejectButtonDisplay: true, customizeButtonDisplay: true, acceptButtonColor: "#FFFFFF", acceptButtonCaptionColor: "#000000", rejectButtonColor: "#1F1F1F", rejectButtonCaptionColor: "#FFFFFF", customizeButtonColor: "#1F1F1F", customizeButtonCaptionColor: "#FFFFFF", textColor: "white", backgroundColor: "#010101", }, }; // Load the banner UI only after the app has rendered, so it doesn't // flash on the empty page before React mounts. Tracker blocking is // unaffected: the autoblocking script above runs synchronously and // keeps trackers gated until consent is given. window.addEventListener("load", function () { var s = document.createElement("script"); s.src = "https://cdn.iubenda.com/cs/iubenda_cs.js"; s.charset = "UTF-8"; s.async = true; document.head.appendChild(s); }); /* Match iubenda banner buttons to the site's pill buttons (Inter 600, 100px radius). Colors come from _iub.csConfiguration.banner; iubenda applies them inline, so only shape/typography live here. */ /* iubenda injects its own stylesheet at runtime with !important rules up to (1,2,0) specificity — the body prefix + extra class keep these at (1,2,1) so they win regardless of injection order. */ body #iubenda-cs-banner .iubenda-cs-opt-group .iubenda-cs-accept-btn, body #iubenda-cs-banner .iubenda-cs-opt-group .iubenda-cs-reject-btn, body #iubenda-cs-banner .iubenda-cs-opt-group .iubenda-cs-customize-btn { border-radius: 100px !important; font-family: "Inter", sans-serif !important; font-weight: 600 !important; font-size: 14px !important; padding: 10px 20px !important; } !(function (e, s) { e.twq || ((s = e.twq = function () { s.exe ? s.exe.apply(s, arguments) : s.queue.push(arguments); }), (s.version = "1.1"), (s.queue = [])); })(window); window.X_ADS_PIXEL_ID = window.X_ADS_PIXEL_ID || "qpz0j"; twq("config", window.X_ADS_PIXEL_ID); (function (t) { var u = t.createElement("script"); u.async = true; // ads-twitter.com is also in the iubenda autoblocking blocklist, and // its US-framework check wrongly re-blocks it after activation. // data-cmp-ab="2" marks it as already consent-approved so the // autoblocker skips it — safe because this script only runs once // iubenda has activated it (consent given or not required). Must be // set BEFORE src: the autoblocker wraps the src setter. u.setAttribute("data-cmp-ab", "2"); u.src = "https://static.ads-twitter.com/uwt.js"; var a = t.getElementsByTagName("script")[0]; a.parentNode.insertBefore(u, a); })(document); !function(w){if(!w.rdt){var p=w.rdt=function(){p.sendEvent?p.sendEvent.apply(p,arguments):p.callQueue.push(arguments)};p.callQueue=[]}}(window);rdt('init','a2_j1ajqrb3b9if');rdt('track', 'PageVisit'); (function(d){var t=d.createElement("script");t.src="https://www.redditstatic.com/ads/pixel.js?pixel_id=a2_j1ajqrb3b9if";t.async=true;var s=d.getElementsByTagName("script")[0];s.parentNode.insertBefore(t,s);})(document); (function (w) { if (w.oaiq) return; var q = function () { q.q.push(arguments); }; q.q = []; w.oaiq = q; })(window); window.OPENAI_ADS_PIXEL_ID = window.OPENAI_ADS_PIXEL_ID || "YG59t983QJt5fthXPitEJV"; oaiq("init", { pixelId: window.OPENAI_ADS_PIXEL_ID, }); (function (d) { var js = d.createElement("script"); js.async = true; js.src = "https://bzrcdn.openai.com/sdk/oaiq.min.js"; var f = d.getElementsByTagName("script")[0]; f.parentNode.insertBefore(js, f); })(document); You need to enable JavaScript to run this app. /** * Configuration Variables - CHANGE THESE! */ // const MIXPANEL_PROJECT_TOKEN = YOUR_MIXPANEL_PROJECT_TOKEN; // e.g. "67e8bfdec29d84ab2d36ae18c57b8535" const MIXPANEL_PROXY_DOMAIN = window.location.origin + "/.netlify/functions/m"; // e.g. "https://proxy-eoca2pin3q-uc.a.run.app" /** * Set the MIXPANEL_CUSTOM_LIB_URL - No need to change this */ // const MIXPANEL_CUSTOM_LIB_URL = MIXPANEL_PROXY_DOMAIN + "/client"; (function (f, b) { if (!b.__SV) { var e, g, i, h; window.mixpanel = b; b._i = []; b.init = function (e, f, c) { function g(a, d) { var b = d.split("."); 2 == b.length && ((a = a[b[0]]), (d = b[1])); a[d] = function () { a.push([d].concat(Array.prototype.slice.call(arguments, 0))); }; } var a = b; "undefined" !== typeof c ? (a = b[c] = []) : (c = "mixpanel"); a.people = a.people || []; a.toString = function (a) { var d = "mixpanel"; "mixpanel" !== c && (d += "." + c); a || (d += " (stub)"); return d; }; a.people.toString = function () { return a.toString(1) + ".people (stub)"; }; i = "disable time_event track track_pageview track_links track_forms track_with_groups add_group set_group remove_group register register_once alias unregister identify name_tag set_config reset opt_in_tracking opt_out_tracking has_opted_in_tracking has_opted_out_tracking clear_opt_in_out_tracking start_batch_senders people.set people.set_once people.unset people.increment people.append people.union people.track_charge people.clear_charges people.delete_user people.remove".split( " " ); for (h = 0; h (function (f) { var e = f.createElement("script"); e.type = "text/javascript"; e.async = true; e.src = "https://cdn.mxpnl.com/libs/mixpanel-2-latest.min.js"; var g = f.getElementsByTagName("script")[0]; g.parentNode.insertBefore(e, g); })(document); window.plausible = window.plausible || function () { (window.plausible.q = window.plausible.q || []).push(arguments); }; window.plausible("View landing page"); --> window.dataLayer = window.dataLayer || []; function gtag() { dataLayer.push(arguments); } gtag("js", new Date()); gtag("config", isQA ? "G-05520JB102" : "G-2JWHBG87VC"); --> window.dataLayer = window.dataLayer || []; function gtag() { dataLayer.push(argumen...
[Content truncated]

## How to Use This Skill

When working with Kite, reference this skill to:
- Understand the core features and capabilities
- Find the official documentation and examples
- Apply best practices from the UI Vault curation

## Related Resources

Visit the full [UI Vault](https://en970.github.io/ui-vault/) for more resources in the Mockup & Promo Video Tools category.

---
*This skill was auto-generated from [Kite](https://kite.video/) — a UI Vault curated resource.*
