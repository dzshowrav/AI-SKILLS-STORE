# Claude Check

Use the `claude-check` skill.

Create a read-only checks-and-balances review prompt for Claude that verifies repo/worktree truth, PR diff truth, receipt truth, test truth, CI truth, Linear/ticket truth, canonical wiring, generated-artifact scope, and strategy. The output should be a copy-paste prompt, not an implementation.
