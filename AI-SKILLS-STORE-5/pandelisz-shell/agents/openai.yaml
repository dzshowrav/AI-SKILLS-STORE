interface:
  display_name: "Shell Commands"
  short_description: "Runs the rest of a /shell request as a literal shell command. Use only when the user explicitly invokes /shell and wants the following text executed directly in the terminal"
  icon_small: ""
  icon_large: ""
  default_prompt: "Run this as a literal shell command."
