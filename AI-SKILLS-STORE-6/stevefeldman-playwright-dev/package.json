{
  "name": "playwright-dev",
  "version": "1.0.0",
  "description": "Playwright browser and API check skill for Claude Code",
  "main": "run.js",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "setup:all-browsers": "npm install && npx playwright install chromium firefox webkit"
  },
  "dependencies": {
    "@evinced/js-playwright-sdk": "^2.44.0",
    "playwright": "^1.50.0",
    "zod": "^3.22.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
